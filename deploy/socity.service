# Legacy systemd unit (replaced by PM2 + ecosystem.config.cjs)
# deploy.sh disables this service if it was installed earlier.
#
# Use PM2 instead:
#   pm2 start /var/www/socity/ecosystem.config.cjs --env production
#   pm2 save && pm2 startup
